module.exports = (global, message) => {
    console.log('Bot online!');
    /*
    const client = global.client;
    global.client.user.setActivity('Over You', {type: 'WATCHING'});

    var firstItem = null;
    for(var x in client.guilds) { // if you have named properties
        firstItem = client.guilds[x];
        break;
    }
    */
    //client.emit("guildCreate", client.guilds.array()[0]);
}